name: clean
channels:
  - conda-forge
  - pytorch
dependencies:
  - python=3.10
  - numpy
  - pandas
  - matplotlib
  - pytorch
  - torchvision
  - cpuonly
  - gym                # OpenAI Gym (older version, still useful)
  - tqdm
  - jupyterlab
